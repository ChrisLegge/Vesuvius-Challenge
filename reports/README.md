# Reports

This folder contains lightweight reports and audit evidence.

`production_audit_report.txt` records the final production notebook checks:

- ensemble weights
- active model selection
- training configuration
- preprocessing thresholds
- test-time augmentation
- inference time budget
- architecture constants
- syntax validation
- degradation levels

The audit report is useful for reviewers because it shows the production notebook was checked systematically before publication.
